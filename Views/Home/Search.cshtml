@{
    ViewData["Title"] = "Search";
    string[] TableHeaders = new string[] { "Company Name", "Topic", "Degree", "Description", "Company Email", "Date Posted" };

}

<h1>Search</h1>

<div class="table">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                @{
                    foreach (var head in TableHeaders)
                    {
                        <th>
                            @head
                        </th>
                    }
                }
            </tr>
        </thead>

        <tbody>
            @{
                if (Model != null)
                {
                    foreach (var Data in Model)
                    {
                        <tr>
                            <td>@Data.CompanyName</td>
                            <td>@Data.Topic</td>
                            <td>@Data.Degree</td>
                            <td>@Data.Description</td>
                            <td>@Data.Email</td>
                            <td>@Data.Date</td>
                        </tr>
                    }
                }
            }
        </tbody>
    </table>
</div>

